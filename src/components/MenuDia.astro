---
/**
 * Componente de tarjeta de día del menú
 * Muestra los platos de un día específico
 */
interface Props {
  dia: string;
  fecha: string;
  primerPlato: string;
  segundoPlato: string;
  postre: string;
  esHoy?: boolean;
}

const { dia, fecha, primerPlato, segundoPlato, postre, esHoy = false } = Astro.props;
---

<div class={`card ${esHoy ? 'ring-2 ring-blue-500 bg-blue-50' : ''}`}>
  <!-- Encabezado del día -->
  <div class="flex items-center justify-between mb-4">
    <div>
      <h3 class={`text-lg font-bold ${esHoy ? 'text-blue-900' : 'text-gray-900'}`}>
        {dia}
      </h3>
      <p class="text-sm text-gray-500">{fecha}</p>
    </div>
    {esHoy && (
      <span class="bg-blue-600 text-white text-xs font-semibold px-3 py-1 rounded-full">
        Hoy
      </span>
    )}
  </div>

  <!-- Platos -->
  <div class="space-y-3">
    <!-- Primer plato -->
    <div class="flex gap-3">
      <div class="flex-shrink-0 w-8 h-8 bg-orange-100 rounded-lg flex items-center justify-center">
        <span class="text-lg">🥗</span>
      </div>
      <div class="flex-1">
        <p class="text-xs text-gray-500 font-medium mb-0.5">Primer plato</p>
        <p class="text-sm text-gray-900">{primerPlato}</p>
      </div>
    </div>

    <!-- Segundo plato -->
    <div class="flex gap-3">
      <div class="flex-shrink-0 w-8 h-8 bg-red-100 rounded-lg flex items-center justify-center">
        <span class="text-lg">🍖</span>
      </div>
      <div class="flex-1">
        <p class="text-xs text-gray-500 font-medium mb-0.5">Segundo plato</p>
        <p class="text-sm text-gray-900">{segundoPlato}</p>
      </div>
    </div>

    <!-- Postre -->
    <div class="flex gap-3">
      <div class="flex-shrink-0 w-8 h-8 bg-purple-100 rounded-lg flex items-center justify-center">
        <span class="text-lg">🍎</span>
      </div>
      <div class="flex-1">
        <p class="text-xs text-gray-500 font-medium mb-0.5">Postre</p>
        <p class="text-sm text-gray-900">{postre}</p>
      </div>
    </div>
  </div>
</div>
